# inputs: array , N(size)
# output: array of size N, where each subset should be equally likely


# Example: A = [1, 2, 4, 5]
# N = 3 -> [1, 2, 4], [1, 2, 5], [1, 4, 5], [2, 4, 5],

# Example: A = [1, 2, 4]]
# N = 2 -> [1, 2], [1, 4], [2, 4]

# brute force approach:
#

def sample_data(arr, n):

    # loop from 0 to len(arr) - (n-1)
    # loop from

    return arr